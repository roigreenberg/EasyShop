<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.roigreenberg.easyshop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:userName=".MainActivity">
            <intent-filter>
                <action android:userName="android.intent.action.MAIN" />

                <category android:userName="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:userName="android.intent.action.VIEW" />

                <category android:userName="android.intent.category.DEFAULT" />
                <category android:userName="android.intent.category.BROWSABLE" />

                <data
                    android:host="easyshop.roigreenberg.com"
                    android:pathPrefix="/add_new_list"
                    android:scheme="http" />
                <data
                    android:host="easyshop.roigreenberg.com"
                    android:pathPrefix="/add_new_list"
                    android:scheme="https" />
            </intent-filter>
        </activity>
        <activity
            android:userName=".AddItemActivity"
            android:label="@string/add_item_activity_name"
            android:windowSoftInputMode="stateAlwaysVisible"/>
        <activity
            android:userName=".EditItemActivity"
            android:label="@string/add_item_activity_name"
            android:windowSoftInputMode="stateAlwaysVisible"/>
        <activity
            android:userName=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:userName="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:userName=".ListActivity"
            android:label="@string/title_activity_list"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:userName="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
    </application>

</manifest>